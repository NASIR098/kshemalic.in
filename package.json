{
  "devDependencies": {
    "wrangler": "^4.110.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.3"
  },
  "scripts": {
    "dev": "npm run --prefix shining-series dev",
    "build": "npm run --prefix shining-series build",
    "deploy": "wrangler pages deploy",
    "generate-types": "wrangler types"
  }
}